import React from "react";
import styles from "./header.module.css";

export default function Header() {
  return <>
    <img className={`${styles.heroImage}`} src="src/assets/home/Hero 2.png" alt="" />
  </>;
}
